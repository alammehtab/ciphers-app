import React from 'react'

const TextArea=()=>{
    return(
        <textarea rows={10} cols={60} style={{fontSize:'large', fontWeight:'bolder', textAlign: 'center'}} placeholder='Enter some text here to encrypt'/>
    )
}

export default TextArea;